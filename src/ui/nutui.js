import Vue from 'vue';
import { Dialog, Picker, Button, BackTop, Icon, SearchBar } from '@nutui/nutui';

Dialog.install(Vue);
Picker.install(Vue);
Button.install(Vue);
BackTop.install(Vue)
Icon.install(Vue);
SearchBar.install(Vue)